#usda 1.0
(
    "WARNING: THIS FILE IS GENERATED BY usdGenSchema.  DO NOT EDIT."
    fallbackPrimTypes = {
        token[] AecoClashResult = ["Scope"]
        token[] AecoClashTest = ["Scope"]
    }
)

class AecoClashTest "AecoClashTest" (
    apiSchemas = ["CollectionAPI:setA", "CollectionAPI:setB", "CollectionAPI:members"]
    customData = {
        string userDocBrief = "A coordination test with two native USD collections and mesh/exact route selection."
    }
)
{
    double aeco:clash:clearance = 0 (
        customData = {
            string userDocBrief = "Metres."
        }
    )
    uniform token aeco:clash:method = "mesh" (
        allowedTokens = ["mesh", "exact"]
        customData = {
            string userDocBrief = "Measurement route."
        }
    )
    double aeco:clash:tolerance = 0.001 (
        customData = {
            string userDocBrief = "Metres."
        }
    )
    string aeco:id = "" (
        customData = {
            string userDocBrief = """Stable unique identity (lowercase hyphenated UUID
        recommended) — groups are addressable artifacts (systems in O&M
        registries, compartments in fire strategies)."""
        }
    )
    uniform token collection:members:expansionRule = "explicitOnly" (
        customData = {
            string userDocBrief = """Schema-level fallback: membership is exactly the
        targeted prims."""
        }
    )
    uniform token collection:setA:expansionRule = "explicitOnly"
    uniform token collection:setB:expansionRule = "explicitOnly"
}

class AecoClashResult "AecoClashResult" (
    customData = {
        string userDocBrief = "A pair finding, distance witness and uncertainty provenance beneath its test."
    }
)
{
    double aeco:clash:distance = 0 (
        aecoDerived = true
        customData = {
            string userDocBrief = "Metres."
        }
    )
    rel aeco:clash:elements (
        aecoDerived = true
        customData = {
            string userDocBrief = "Exactly two distinct existing element referents."
        }
    )
    string aeco:clash:evidence = "" (
        aecoDerived = true
        customData = {
            string userDocBrief = "JSON provenance: body paths, roles, approximation, stamps, uncertainty method and mesh measurement limitations."
        }
    )
    uniform token aeco:clash:kind = "hard" (
        aecoDerived = true
        allowedTokens = ["hard", "clearance", "touching"]
        customData = {
            string userDocBrief = "Geometric finding category, not product classification."
        }
    )
    point3d aeco:clash:point = (0, 0, 0) (
        aecoDerived = true
        customData = {
            string userDocBrief = "World-space location in metres: mesh witness or exact camera-framing midpoint, as declared in evidence."
        }
    )
    uniform token aeco:clash:status = "new" (
        allowedTokens = ["new", "active", "reviewed", "approved", "resolved"]
        customData = {
            string userDocBrief = "Review driver."
        }
    )
    double aeco:clash:uncertainty = 0 (
        aecoDerived = true
        customData = {
            string userDocBrief = "Metres."
        }
    )
    double aeco:clash:volume = 0 (
        aecoDerived = true
        customData = {
            string userDocBrief = "Cubic metres."
        }
    )
}

